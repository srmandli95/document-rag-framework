[pytest]
markers =
    slow: tests that load local machine-learning models
filterwarnings =
    ignore:Using `httpx` with `starlette\.testclient` is deprecated.*
    ignore:'crypt' is deprecated.*:DeprecationWarning:passlib\..*
